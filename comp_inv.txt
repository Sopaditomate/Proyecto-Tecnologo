import { useState, useEffect } from 'react';
import axios from 'axios';
import { Container } from 'react-bootstrap';  // Puedes mantener esto para el Container, pero ya no necesitas Row y Col si no los usas
import "./inventory.css";

const InventoryManagement = () => {
  const [inventory, setInventory] = useState([]);

  useEffect(() => {
    fetchInventory();
  }, []);

  const fetchInventory = async () => {
    try {
      // Usando el nuevo endpoint proporcionado
      const response = await axios.get('http://localhost:5001/api/inventario/');
      setInventory(response.data);  // Guardamos los datos en el estado
    } catch (error) {
      console.error('Error al obtener el inventario:', error);
    }
  };

  return (
    <Container>
      <h1 className="text-2xl font-semibold mb-6">Gestión de Inventario</h1>
      <table className="min-w-full bg-white border border-gray-300 shadow-md rounded-lg">
        <thead>
          <tr className="bg-gray-100">
            <th className="px-4 py-2 text-left font-semibold text-gray-700">ID Inventario</th>
            <th className="px-4 py-2 text-left font-semibold text-gray-700">Materia Prima</th>
            <th className="px-4 py-2 text-left font-semibold text-gray-700">Cantidad</th>
            <th className="px-4 py-2 text-left font-semibold text-gray-700">Unidad</th>
            <th className="px-4 py-2 text-left font-semibold text-gray-700">Tipo</th>
          </tr>
        </thead>
        <tbody>
          {inventory.map((item) => (
            <tr key={item.ID_INVENTARIO} className="border-b hover:bg-gray-50">
              <td className="px-4 py-2 text-gray-800">{item.ID_INVENTARIO}</td>
              <td className="px-4 py-2 text-gray-800">{item.MATERIA_PRIMA}</td>
              <td className="px-4 py-2 text-gray-800">{item.CANTIDAD}</td>
              <td className="px-4 py-2 text-gray-800">{item.UNIDAD}</td>
              <td className="px-4 py-2 text-gray-800">{item.TIPO}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </Container>
  );
};

export default InventoryManagement;
